{
  "name": "ifarm-security-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev:web": "pnpm --filter @ifarm-security/web dev",
    "dev:api": "pnpm --filter @ifarm-security/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=24"
  }
}
